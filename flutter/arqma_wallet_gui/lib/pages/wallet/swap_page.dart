import 'package:flutter/material.dart';

import 'wallet_page_stub.dart';

class SwapPage extends StatelessWidget {
  const SwapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const WalletPageStub(title: 'Swap');
  }
}
