import 'package:flutter/material.dart';

import 'wallet_page_stub.dart';

class StakingPoolsPage extends StatelessWidget {
  const StakingPoolsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const WalletPageStub(title: 'Staking pools');
  }
}
